name: Veracode Workflow

on:
  push:
    branches:
      - master

jobs:
  veracode:
    uses: LaisLora/github-actions-templates/.github/workflows/veracode-analysis.yml@main
    with:
      project-path: "."  # Diretório do projeto
      project-name: "${{ github.repository }}"  # Nome do repositório
    secrets: inherit
