name: Veracode Workflow

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  veracode:
    uses: LaisLora/github-actions-templates/.github/workflows/veracode-analysis.yml@master
    secrets: inherit
    with:
      build-id: ${{ github.run_id }}
      project-name: "${{ github.repository }}"  # Nome do repositório
