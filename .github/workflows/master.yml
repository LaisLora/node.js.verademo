name: Veracode

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  veracode:
    uses: LaisLora/github-actions-templates/.github/workflows/veracode-analysis.yml@master
    with:
      config-path: .github/workflows/veracode-analysis.yml
    secrets:
      token: ${{ secrets.ACTIONS_DEPLOY_KEY }}
