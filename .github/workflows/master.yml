name: Veracode 

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  veracode:
    uses: LaisLora/github-actions-templates/blob/master/.github/workflows/veracode-analysis.yml@main
    secrets: inherit
    with:
      build-id: ${{ github.run_id }}
      project: "${{ github.repository }}" # Nome do repositório.
 
