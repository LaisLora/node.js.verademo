name: Veracode Analysis

on:
  push:
    branches:
      - master
jobs:
  build:
    uses: LaisLora/github-actions-templates/.github/workflows/veracode-analysis.yml@main
    with:
      build-id: ${{ github.run_id }}
      environment: master  # Ajustar conforme o ambiente
      namespace: master  # Ajustar conforme o app
      project: ${{ github.repository }}  # Nome do repositório
